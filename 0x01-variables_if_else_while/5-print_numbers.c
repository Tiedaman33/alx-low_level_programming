#include <stdio.h>
/**
 *main - Entry point
 *Description: 'check for number if it is postive or negative'
 *Return: always 0
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
